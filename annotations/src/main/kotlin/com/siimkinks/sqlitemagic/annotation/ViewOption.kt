package com.siimkinks.sqlitemagic.annotation

/** SQLite view definition options. */
enum class ViewOption {
  /** Creates the view in the temporary database for the current database connection. */
  TEMPORARY
}